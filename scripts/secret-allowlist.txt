# 误报豁免清单 (secret scanner allowlist)
# 格式: 相对路径:整行内容 —— 每行对应一个 WARNING 命中，行内容须与文件实际行逐字一致。
# 添加时必须在此行上方用 # 写明理由。CRITICAL 命中不接受豁免。
# 示例:
#   # 文档示例中的假 token，非真实凭据
#   README.md:token = "example0000000000000000000000000000"
