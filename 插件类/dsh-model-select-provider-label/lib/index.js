// dsh-model-select-provider-label — host 半区(空)。
// 全部功能在 client 半区(lib/client.js):模型选择器触发按钮显示提供商名、
// 超阈值提供商折叠为级联面板、按前缀分类、三级菜单带返回。
// 本文件仅提供可加载的 host 导出,挂载时 cordis 组合要求包有 main 入口。
export default {
  apply() {
    // no host-side work
  },
}
